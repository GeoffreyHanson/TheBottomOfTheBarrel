// $.getJSON('/articles', (data) => {
//   // For each one
//   // for (let i = 0; i < data.length; i++) {
//   data.forEach((i) => {
//     // Display the apropos information on the page
//     $('#articles').append(
//       `<p data-id='${data[i]._id}'>${data[i].title}<br />${data[i].link}</p>`, // eslint-disable-line
//     );
//   });
// });
